/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SliderMilestoneDirective } from './slider-milestone.directive';

describe('Directive: SliderMilestone', () => {
  it('should create an instance', () => {
    const directive = new SliderMilestoneDirective();
    expect(directive).toBeTruthy();
  });
});
